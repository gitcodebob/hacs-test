"""Orchestrator-driven control application (ADR-002).

Bounded contexts: ``control`` (orchestrator), ``resources``, ``strategy``,
``planning`` and ``market``. This package holds pure application logic, kept
free of Home Assistant wiring.
"""
